# Jenkins Jobs

A job file launches a [pipeline](../pipelines/README.md) with specific parameters. Where a pipeline might wrap a [shared library](../libraries/README.md), a job exposes the configurable parameters when running in the Jenkins UI.
